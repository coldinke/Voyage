Search Voyage memory for context relevant to the current task.
Run: voyage search --context --limit 3 "$ARGUMENTS"
Use the results to inform your approach — reference past sessions that solved similar problems.
